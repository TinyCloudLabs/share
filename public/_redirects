/s/* /viewer.html 200
/share /share.html 200
/viewer /viewer.html 200
# Registry proxying is performed by the authenticated production host from
# the validated trust bundle; a static origin is intentionally not committed.
