{
  "name": "@tinycloud/share-email",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone email-sending Cloudflare Worker for addressed shares: verifies the Node-signed delivery authorization, renders the invitation, sends it through Resend, and enforces one-send-per-authorization in D1.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tinycloud/share-envelope": "*"
  },
  "devDependencies": {
    "@noble/curves": "^1.9.2",
    "@types/node": "^20.19.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
